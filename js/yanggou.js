//:加载商品页面function loadProduct(pno,pageSize){	var reg=/^\d+$/;	if(!reg.test(pno)){		alert("页码格式不正确") ;		return;	}	if(!reg.test(pageSize)){		alert("页码大小格式不正确");	}    $.ajax({    type:"GET",	url:"php/page.php",	data:{pno,pageSize},    dataType:"json",    success:function(data){    	var dogs=data.data;		var html="";		for(var{uid,img,title,subtitle,uname,readCount,            date} of dogs){			html+=`<div class="top clear">					<div class="tu lf">						<img src="${img}" alt="">					</div>					 <div class="wen lf">						<a href="#">						<h2>${title}</h2>						</a>						<p>${subtitle}</p>						<div>							<span>${uname}</span>							<span class="yan"></span>							<span>${readCount}</span>							<span>${date}</span>						</div>					</div>				</div>`		}			 $("#pets").html(html);		//实现分页        var html = "";        pno = parseInt(data.pno);        pageCount = parseInt(data.pageCount);        //上上一页        if(pno-2>0){            html += `<a href="#">${pno-2}</a>`;        }        //上一页        if(pno-1>0){            html += `<a href="#">${pno-1}</a>`;        }        //当前页        html += `<a href="#" class="active">${pno}</a>`;        //下一页    11:57        if(pno+1<=pageCount){            html += `<a href="#">${pno+1}</a>`;        }        //下下一页        if(pno+2<=pageCount){            html += `<a href="#">${pno+2}</a>`        }        $("#fenye").html(html);	},	error:function(){            alert("网络故障请检查")	}  })}loadProduct(1,10); $("#fenye").on("click","a",function(e){ 	e.preventDefault(); 	var pno=parseInt($(this).html()); 	loadProduct(pno,10); });$(function(){  $("#header").load("header.html");   $("#footer").load("footer.html");});